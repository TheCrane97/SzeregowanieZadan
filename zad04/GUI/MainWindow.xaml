<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Label x:Name="label" Content="Zmodyfikowany Johnson" HorizontalAlignment="Left" Margin="173,69,0,0" VerticalAlignment="Top" Height="57" Width="480" FontSize="36"/>
        <DataGrid x:Name="dataGrid" ItemsSource="{Binding}" HorizontalAlignment="Left" Height="25" Margin="65,203,0,0" VerticalAlignment="Top" Width="654"/>
        <DataGrid x:Name="dataGridM2" ItemsSource="{Binding}" HorizontalAlignment="Left" Height="25" Margin="65,228,0,0" VerticalAlignment="Top" Width="654"/>

        <DataGrid x:Name="dataGridM3" ItemsSource="{Binding}" HorizontalAlignment="Left" Height="25" Margin="65,253,0,0" VerticalAlignment="Top" Width="654"/>

        <DataGrid x:Name="dataGrid1" ItemsSource="{Binding}"  HorizontalAlignment="Left" Height="25" Margin="65,278,0,0" VerticalAlignment="Top" Width="654" />
        <Button x:Name="button" Content="Wyświetl" HorizontalAlignment="Left" Margin="644,370,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click"/>

    </Grid>
</Window>
